# 上游集成评审：2026-09-06

本次集成固定为 `8d206292727b` → `4f0bd5f31fd1`（32 个提交，含合并提交），
从 MaxCode `de9a30950a0d` 创建 `integrate/upstream-20260906`。
用户于 2026-09-07 确认冲突处理方案。上游版本标签为 v0.30.3，MaxCode 保持 0.30.4。

## 冲突处理

`pnpm upstream:impact` 返回 2，报告 102 个上游改动文件及下游重叠/行为热点。
三方合并产生 28 个冲突文件，其中 10 个为语言文件。

- 版本与品牌：保留 MaxCode 名称、0.30.4 版本、应用标识和个人仓库更新源。
- 通知：采用上游事件分类、批量通知、隐私文本和回放抑制；通知标题、测试通知及
  新增用户提示使用 MaxCode。macOS 通知标识从应用配置读取。
- OpenCode：上游 1.18.29 的六个平台均未提供注册表 SHA256；GitHub release API
  查询返回 HTTP 403，无法取得可靠的新校验值，按用户确认的回退方案保留已有完整
  SHA256 的 1.18.25。其他智能体版本与模型目录采用上游更新。
- 备份、Forge、导入服务：对共同基点与本地版本运行相同 Rust 格式化后比较，确认
  备份核心、恢复、外部存储、Web handler、Forge 和导入服务的本地差异只是格式。
  用规范化后的三个版本重新执行三方合并，保留备份错误中的 MaxCode 文本，以及
  会话导入测试等待全局锁的本地修复；没有批量选择 ours/theirs。
- 删除会话：采用上游显式勾选恢复策略，自动扫描、整文件夹导入继续跳过删除项。

## 重叠行为评审

| 上游改动 | 下游交集及处理 |
| --- | --- |
| `f558c563` 备份与代理存储修复 | 新增恢复锁和退出中进程保护。MaxCode 已将连接状态统一到 SessionState，因此恢复检查改为读取该状态；被写锁占用的会话、未退出的子进程均视为活跃，避免等待会话锁造成锁顺序问题。保留 Connecting 看门狗、只读探测及最近空闲连接保活。 |
| `0bad54ff`、`93892a0e`、`8f8acc5e` 通知与设置 | 保留品牌及全部运行时统计、Web 重连逻辑；接入上游通知接口和批次合并，补查无文本冲突的新提问通知标题与全部语言的测试通知。 |
| `e137f0ea` 消息附件 | 接入统一用户消息块转换、steer 附件显示和持久化去重；保留运行时 ID 重绑定、生成耗时统计、标签卸载及文件夹分栏。 |
| `5f6f2a95` 软删除恢复 | 恢复既有会话 ID，并归入用户选择的目标文件夹；不修改下游标题锁定逻辑，沿用上游针对隐式导入、子会话和幂等性的测试。 |
| `221d5440`、`77e65bc6`、`f4d00480`、`4f8c5821` Forge 与任务错误 | 增加 Gitea/Forgejo 与有界错误详情；保留本地格式和任务通知品牌，未涉及自动化会话标题实现。 |
| `35fe8af1`、`c37cb831` 智能体与目录 | 采用新版模型目录与对应断言；保留 OpenCode 校验约束、专用标题服务、Pi 扩展配置及 Grok 定制。 |
| `18a69935` 至 `4f0bd5f3` 壁纸市场 | 合入壁纸来源标记、下载和双端路由；保留 UiPreferences 持久化、系统字体默认值、共享会话和标题设置路由。 |
| 发布与配置热点 | 仅保留本地版本字段；签名配置、产物名称、更新地址和图标保持下游版本。 |

OpenCode 下载校验新增独立契约并登记热点。通知品牌和恢复状态适配扩展已有的
独立品牌、ACP 生命周期契约。

## 验证

- `pnpm upstream:guard`：16 个契约文件、59 项测试通过。
- `pnpm eslint .`：通过。
- `pnpm test`：459 个文件、6,338 项测试通过。
- `pnpm build`：静态导出通过。
- `cargo test --features test-utils`：单元及集成测试合计 3,610 项通过，2 项原有
  手动测试保持忽略。
- `cargo test --no-default-features --bin codeg-server --lib`：3,523 项通过，
  1 项原有手动测试保持忽略。
- 桌面 `cargo clippy --all-targets --features test-utils -- -D warnings`：通过。
- 服务器 `cargo clippy --no-default-features --bin codeg-server --lib -- -D warnings`：通过。
- MCP `cargo check --no-default-features --bin codeg-mcp` 与相同目标的 Clippy：通过。
- 十种语言的下游新增键、定制值及本次上游新增键完整性检查：通过。
- 提交前再次运行影响扫描，仍按预期返回 2；本文件记录对应的重叠评审。

Rust 输出已有的 `proc-macro-error2` 未来兼容性提示，桌面调试二进制链接器提示
展开信息过大；均未导致测试或 Clippy 失败。
